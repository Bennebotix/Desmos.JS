class Calculator {
  constructor() {
    const preview = {actions: !0, expressions: !1, settingsMenu: !1};
    const editor = {actions: !0};

    var calcElmnt = document.querySelector
    var calculator = Desmos.GraphingCalculator(calcElmnt, editor);
  }
}
